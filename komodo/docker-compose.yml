################################
# 🦎 KOMODO COMPOSE - MONGO 🦎 #
################################

## This compose file will deploy:
##   1. MongoDB
##   2. Komodo Core

services:
  core:
    image: ghcr.io/moghtech/komodo-core:${COMPOSE_KOMODO_IMAGE_TAG:-2}
    # container_name: ${CORE_CONTAINER_NAME:-core}

    restart: unless-stopped
    init: true

    depends_on:
      - mongo

    ports:
      - "${BIND_ADDRESS:-0.0.0.0}:${PORT:-9120}:9120"
    # env_file: ./compose.env
    extra_hosts:
      - host.docker.internal:host-gateway

    env_file:
      - path: .env
        required: true
    environment:
      KOMODO_DATABASE_ADDRESS: mongo:27017
      KOMODO_DATABASE_USERNAME: ${KOMODO_DATABASE_USERNAME:-admin}
      KOMODO_DATABASE_PASSWORD: ${KOMODO_DATABASE_PASSWORD:?KOMODO_DATABASE_PASSWORD is required}
    labels:
      com.centurylinklabs.watchtower.enable: ${CORE_WATCHTOWER_ENABLE:-true}

    volumes:
      ## Attach the Core / Periphery communication keys
      - ${MNT_DIR:-/mnt/docker}/komodo/keys:/config/keys
      ## Store dated backups of the database - https://komo.do/docs/setup/backup
      - ${MNT_DIR:-/mnt/docker}/komodo/backups:/backups
      ## Store sync files on server
      - ${MNT_DIR:-/mnt/docker}/komodo/syncs:/syncs
      ## Optionally mount a custom core.config.toml
      - ${CONFIG_DIR:-/mnt/docker/komodo/core.config.toml}:/config/config.toml
      ## Optionally mount custom root CA certificate to trust
      # - /path/to/root_ca.crt:/usr/local/share/ca-certificates/root_ca.crt
    ## Allows for systemd Periphery connection at
    ## "http://host.docker.internal:8120"

  mongo:
    image: mongo:${MONGO_IMAGE_TAG:-8.0}
    # container_name: ${MONGO_CONTAINER_NAME:-mongo}

    restart: unless-stopped
    # ports:
    #   - 27017:27017
    command: --quiet --wiredTigerCacheSizeGB 0.25
    # extra_hosts:
    #   - host.docker.internal:host-gateway

    environment:
      MONGO_INITDB_ROOT_USERNAME: ${KOMODO_DATABASE_USERNAME:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${KOMODO_DATABASE_PASSWORD:?KOMODO_DATABASE_PASSWORD is required}

    labels:
      com.centurylinklabs.watchtower.enable: ${MONGO_WATCHTOWER_ENABLE:-false}
      komodo.skip: # Prevent Komodo from stopping with StopAllContainers

    volumes:
      - ${MNT_DIR:-/mnt/docker}/komodo/mongo/data:/data/db
      - ${MNT_DIR:-/mnt/docker}/komodo/mongo/config:/data/configdb
