services:
  redis:
    image: redis:${IMAGE_TAG:-latest}
    restart: unless-stopped
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --save ""
      --appendonly no

    ports:
      - "172.17.0.1:6379:6379"
